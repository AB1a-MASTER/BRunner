BRunner - Current Development Handoff
Updated: 2026-07-27

PURPOSE

This file is the concise starting point for the next development session. Read
`README.md` and then `docs/BRUNNER_MASTER_ROADMAP.md`. The README owns the
product boundary; the roadmap owns milestone order and current priority. This
handoff records the immediate working state and must not become a second
roadmap.

CURRENT PRODUCT BOUNDARY

- BRunner currently targets Chrome or a compatible Chromium browser plus a
  local Windows companion application.
- The extension owns workflow authoring/execution, tabs/pages, browser actions,
  the mapper engine, and post-action checks.
- Graph Studio is the only supported workflow-authoring surface. Finalized
  workflows use one canonical mapper-graph schema and graph runtime.
- Sequential Studio is deprecated and disabled under B19. Normal launches open
  Graph Studio, its former URL redirects without loading authoring/run code,
  and `BRunner/studio/` remains dormant until the final pre-V2 cleanup after
  integrated V1 source acceptance.
- Do not equate the deprecated Sequential Studio UI with the provisional legacy
  linear executor. Retain the executor wherever a supported transitional path
  still depends on it, and remove it only through a separate dependency audit.
- The companion owns local workflow persistence, approved-folder operations,
  process status, and explicitly requested visible operating-system assistance.
- The product assumes one cooperating local user and one selected browser
  profile at a time.
- Cross-platform desktop support, remote/multi-user service, headless/cloud
  operation, and unattended deployment are not current scope.

TRUST AND DATA BOUNDARY

- The project does not currently guarantee credential protection, sensitive-data
  handling, complete redaction, secret-safe logs, or privacy-safe saved maps.
- Existing masking is defensive behavior, not a supported security promise.
- Use synthetic, disposable data for all development and acceptance. Do not use
  real passwords, tokens, payment data, private messages, or confidential files.
- The companion target is a cooperative one-profile host lock using a stable,
  non-secret per-profile instance ID. There is no pairing key or authentication
  claim. The source pairing flow and single-active-connection lock are now
  covered by real loopback WebSocket contract tests.
- Do not describe the current pairing flow as a security boundary.

CURRENT PRIORITY

Shared foundation, Companion source-checkout acceptance, and mapper-engine
acceptance passed on 2026-07-20 after the reported foreground-fallback,
loaded-window, body-ancestry, and verification issues were fixed and rerun. The
extension/host round trip passed with `extension-host-roundtrip.txt |
extension-host-roundtrip.txt | 1`.

The most recent recorded source verification on 2026-07-27 passed 445
JavaScript tests and 183 Python tests; Graph Studio remains the verified
191-module build and the cache-safe focused Navigate/server set passed 21/21.
Rerun the relevant gates after implementation changes rather than treating
those counts as permanent. Navigate v2 passed both its unpacked-extension
success case and its stopped-server red error route. Its user-controlled commit
hash is the only remaining Node 1 closeout item.

Focus on:

1. using `docs/NODE_IMPLEMENTATION_STATUS.md` as the canonical work tracker,
   evidence ledger, and completed 94-node disposition inventory;
2. preserving completed B10-B12 so Graph authoring, saved JSON, validation,
   autocomplete, value coercion, workflow preparation, and execution continue
   to use one canonical semantic path;
3. preserving completed B19 isolation while retaining dormant source for
   pre-V2 cleanup;
4. recording the accepted Navigate slice in the user-controlled quick commit;
   and
5. only after that commit, continuing Scroll, Tab Control, Resolve Element,
   Check Element State, and Wait for Condition in catalog order.

The existing Mapper Inspector is an engineering diagnostic surface for this
work. Do not spend the current milestone polishing it into a final saved-map
product. A polished saved-map viewer, its final review workflows, responsive UI,
and visual design are deferred to V2.

CURRENT TODO SEQUENCE

1. COMPLETE: `docs/NODE_IMPLEMENTATION_STATUS.md` and
   `BRunner/nodes/catalog.js` inventory all 94 finalized nodes and account for
   every provisional action type.
2. COMPLETE: Graph Studio consolidation items B10-B12.
3. COMPLETE: B19 disables Sequential Studio entry points and authoring/run
   access while retaining `BRunner/studio/` as dormant reference source.
4. ACCEPTED, COMMIT PENDING: Navigate passed focused Graph Studio success and
   stopped-server error-route acceptance. Record the user-controlled commit
   hash, then correct and integrate the remaining Phase 1 adapters and nodes in
   catalog order beginning with Scroll.
5. Continue node Phases 2-7 one node at a time, removing provisional types
   absent from the catalog. Every node gets automated tests, a focused workflow
   under `BRunner_Host/Workflows/node_acceptance/`, focused live evidence, an
   end-user catalog entry, a tracker update, and a quick user-controlled commit.
6. Complete the six cross-node acceptance workflows and integrated V1 source
   acceptance.
7. Run the pre-V2 cleanup: remove the already-disabled Sequential Studio and
   its exclusively owned code/tests/docs only after supported Graph, sidebar,
   and runtime paths pass an import/dependency audit.
8. Keep nonessential Inspector/saved-map visual work deferred to V2.
9. Ask the user before any V2 viewer, product-boundary change, packaging,
   installer, or release milestone.

NODE CATALOG RULE

`workflow_nodes_implementation_blueprint.md` is the sole finalized node catalog.
Earlier catalogs and the currently registered nodes describe provisional
development behavior only.

Do not add, rename, consolidate, redesign, or remove provisional nodes before the
node milestone. Mapper-unblocking correctness fixes are the only exception.
During the node milestone, follow the blueprint definition of done and track
upgrade, rewrite/change, add, and removal status. Current actions are reference
code, not a compatibility contract.

Graph Studio is the sole finalized node editor. Its registry-driven editor,
saved mapper-graph JSON, validation, workflow preparation, and background graph
runtime must agree on the same versioned node definitions, ports, routes,
configuration, and output contracts. Runtime dispatch uses `(type, version)`;
reused provisional type IDs start their finalized contract at version 2, and
unknown versions fail closed.

Sequential Studio is not a compatibility target. It is disabled without
deletion; leave the dormant source untouched during node work and remove it only
in the tracked pre-V2 cleanup.

Do not implement graph branching, loops, joins, or error paths in the linear
executor. They require graph traversal, explicit ports, validation, limits, and
cancellation semantics.

RELEASE POLICY

- `release/BRunner-extension.zip`, `release/BRunnerHost.exe`, and any other
  existing packaged outputs are disposable test artifacts.
- They are not an accepted or distributable release.
- Do not rebuild, validate, install-test, polish, sign, or publish release
  artifacts during the current mapper or node work.
- Packaged GUI/extension acceptance is not a current gate.
- Release work starts only after mapper, node, and integrated-source acceptance
  and an explicit user request to begin a release milestone.

WORKING FOUNDATION

The following accepted foundation should be preserved while working on the
active node milestone:

- runtime state, cancellation, navigation, per-run variables, and any legacy
  linear recording/execution infrastructure still required by a supported
  transitional path;
- Graph Studio, graph persistence, legacy adapters, and mapper schema-v3
  `success`/`unresolved` traversal;
- current provisional browser, element, wait, data, file, clipboard, download,
  screenshot, and host-backed actions;
- Windows companion source for workflow storage, approved folders, diagnostics,
  cooperative profile pairing/connection exclusivity, and visible fallback;
- mapper core/coordinator/store, component identities, reconciliation,
  diagnostics, complete operator fixtures, route-safe Inspector resolution, and
  bounded transactional Chrome-storage persistence.

This list records accepted foundation source, not final node or product-release
acceptance. Do not infer that the viewer, finalized node catalog implementation,
security posture, or release is complete.

HISTORICAL CONTEXT - NOT CURRENT TODO

Earlier work implemented and exercised the sequential runtime, graph editing,
Studio UI refinement, companion application foundations, mapper storage and
Inspector experiments, platform-profile fixtures, and packaging experiments.
The old handoff accumulated detailed numbered slices for that work.

Those slices are intentionally summarized here because their former “next”,
“remaining”, packaging, Inspector-polish, and release instructions are obsolete.
Use source history and focused specifications only when implementation detail is
needed, and resolve any scope conflict through the current master roadmap.

ACCEPTANCE EXPECTATIONS

- Test the smallest active slice first, then the relevant deterministic suite.
- Rebuild Graph Studio generated assets whenever source under
  `BRunner/studio-graph-src/` changes.
- For mapper behavior, assert the node-neutral resolver outcome, component
  evidence, page/frame context, and ambiguity decision.
- Live mapper checks must use the repository fixtures and synthetic data.
- The mapper must never turn ambiguity into a guessed resolved component.
- UI polish alone does not close a mapper reliability issue.
- Do not use current release artifacts as acceptance evidence.

IMPORTANT REFERENCES

- Product scope and sequence: `docs/BRUNNER_MASTER_ROADMAP.md`
- Shared extension/Studio work: `docs/FOUNDATION_TODO_STATUS.md`
- Sole node catalog: `workflow_nodes_implementation_blueprint.md`
- Persistent node-program instructions: `AGENTS.md`
- Living node tracker/evidence ledger: `docs/NODE_IMPLEMENTATION_STATUS.md`
- End-user node catalog: `docs/NODE_USER_CATALOG.md`
- Per-node acceptance workflows: `BRunner_Host/Workflows/node_acceptance/`
- Mapper behavior details: `docs/specs/08_MAPPER_RELIABILITY_TRANSITION.md`
- Mapper source checklist/fixtures: `docs/MAPPER_TODO_STATUS.md` and
  `docs/MAPPER_MANUAL_ACCEPTANCE.md` where they agree with the master roadmap
- Companion implementation details: `docs/specs/07_WINDOWS_COMPANION_APP.md`
- User-facing behavior: `docs/BRUNNER_USER_GUIDE.md`

GIT OWNERSHIP

- The user controls Git operations.
- Do not stage, commit, amend, branch, reset, push, or open a pull request unless
  the user explicitly requests that operation.
- Preserve unrelated user changes.

DEVELOPMENT RULES

- Keep changes isolated to the active milestone.
- Preserve shared cancellation, persistence, navigation, and tab-recovery
  infrastructure. Provisional node/workflow compatibility is not required.
- Add behavior through shared registry/schema/mapper contracts, not action-name
  hacks.
- Keep mapper decisions deterministic and fail closed on ambiguity.
- Make no credential, redaction, privacy, or security-auth guarantee.
- Treat pairing as a cooperative profile lock.
- Treat provisional nodes only as implementation reference; the finalized
  blueprint now controls upgrades, rewrites, additions, and removals.
- Use one canonical mapper-graph workflow/node model through Graph Studio,
  persistence, validation, preparation, and execution.
- Keep Sequential Studio disabled but present until the final pre-V2 cleanup;
  do not repair it or use it as acceptance evidence.
- Complete one node at a time, including its acceptance workflow and end-user
  documentation, then hand it to the user for the quick Git commit.
- Defer polished saved-map viewing to V2.
- Perform no release work now.
- Update this handoff and the master roadmap together whenever priority, scope,
  or milestone status changes.
