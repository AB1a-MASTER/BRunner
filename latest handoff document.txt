BRunner - Latest Development Handoff
Updated: 2026-07-09

PURPOSE
This is the starting point for the next development chat. Read this file, then
use Design Doc.txt and docs/ as the source of truth. Preserve all user work and
continue with isolated, tested slices.

GIT OWNERSHIP
- The user controls every Git operation.
- Do not stage, commit, amend, branch, reset, push, or open a PR.
- Provide a commit message only when the user explicitly asks.
- Do not assume the changes described here are committed.
- The latest requested commit message was:

  feat(host): add visual-match fallback

  Add opt-in PyAutoGUI visual recovery after coordinate host
  fallback fails or post-action verification does not pass.

CURRENT MILESTONE
Windows companion transition Phase 6 is functionally accepted. Approved
directory find/write/export workflows pass, visible host fallback passes, and
opt-in PyAutoGUI visual-match fallback passed manual acceptance with Chrome side
UI open. The side-UI visual path is functional but slow. The current
implementation phase is Phase 7 packaging and release cleanup. The
pre-shipping Sequential Studio UI/error-channel code pass is implemented.
Graph Studio view-setting refinement is also implemented so command controls,
panels, nodes, inspector forms, canvas tools, and execution logs scale from the
same density tokens. The user accepted the Studio visual pass as good enough
to proceed. Final packaging now emits exactly two top-level release artifacts:
`release/BRunner-extension.zip` and `release/BRunnerHost.exe`. The packaged
host smoke passed from the final release directory: `--serve-host` entered
host mode, created runtime folders beside the exe, wrote the host log, and
listened on the configured WebSocket port.

Pre-shipping blockers added by the user:
- Sequential Studio view/display option now scales the broader shell, command
  bar, panels, canvas spacing, cards, and form controls.
- Sequential Studio action and workflow-details panels are independently
  collapsible and persist through shared Studio preferences.
- Sequential Studio layout has panel headers, clearer hierarchy, and restored
  canvas space when panels are collapsed.
- Chrome extension panel run failures now show a concise workflow status while
  node/workflow details stay in Studio diagnostics/logs.
- Graph Studio view/display option now uses local Graph density tokens for
  text, controls, icons, node dimensions, panel rails, and log rows instead of
  leaving key elements at fixed pixel sizes.

New planning sources:
- `BRunner_Windows_Companion_Transition_Plan.md`
- `windows_companion_app_design_notes.docx`
- `BRunner_Mapper_Reliability_Transition_Plan.md`
- `workflow_nodes_implementation_blueprint.md`
- `docs/specs/07_WINDOWS_COMPANION_APP.md`
- `docs/specs/08_MAPPER_RELIABILITY_TRANSITION.md`

Forward order:
1. Implement the Windows companion app transition.
2. Implement the mapper reliability transition: shared Mapper Core,
   workflow-scoped component maps, graph schema v3 unresolved routing,
   ComponentRef targets, Chrome-storage MapStore, open Shadow DOM support, and
   dedicated Mapper Inspector.
3. Implement the finalized node list from
   `workflow_nodes_implementation_blueprint.md`, domain by domain.

The sequential runtime and Phase A/Phase B node expansion are complete and live
accepted. The Variables/Data Inspector, autocomplete/validation, extraction UX,
schema-v2 foundation, React Flow Graph Studio, graph persistence, safe v1 upgrade,
canvas organization, bypass behavior, and live graph execution are implemented
and live accepted.

CURRENT TEST BASELINE
- 141 deterministic JavaScript tests pass.
- 98 deterministic Python tests pass from the latest host-backed mapper
  persistence hardening pass.
- `npm run studio:build` succeeds with Vite 8 and extension-safe relative assets.
- PyInstaller `python build_host_ui.py` succeeds and `python release_builder.py`
  produces the final two release artifacts.
- Final packaged-host smoke passed from `release/BRunnerHost.exe`.
- Pairing/auth source pass is implemented after this packaged baseline, but the
  release artifacts have not been rebuilt because builds should run only when
  the user explicitly asks.

Do not lower this baseline. Rebuild Graph Studio after every source change under
`BRunner/studio-graph-src/`.

COMPLETED FOUNDATION

1. Reliable sequential runtime
- Semantic recording targets, fallback resolution, page recovery, and diagnostics.
- Multi-tab logical references, tab recovery, open/close/switch behavior.
- Shared Studio/sidebar runtime state and cooperative cancellation.
- Native workflow persistence, rename, duplicate, and synchronization.
- Fresh per-run variables and strict `{{variable}}` expression resolution.

2. Node engine and external operations
- Registry-driven browser, element, wait, extraction, data, and transform nodes.
- HTTP requests with cancellation and safe logging.
- Permission-gated clipboard read/write.
- Virtual and allowlisted local-file upload.
- Download waiting and visible-tab screenshots.
- Phase A and Phase B are complete and live accepted.

3. Sequential Studio UX slices
- Variables/Data Inspector with seed/current/last-run values and safe summaries.
- Object/list/table previews, search, copy expression, and copy value.
- Registry-driven required-field and target validation.
- Variable autocomplete limited to seeds and outputs available before the node.
- Unknown and forward expression references block save/run.
- Extraction selector guidance, contextual fields, and last-run node samples.

GRAPH SCHEMA AND MIGRATION

Files:
- `BRunner/core/workflowSchema.js`
- `BRunner/core/workflowUtils.js`
- `BRunner_Host/workflow_storage.py`
- `BRunner_Host/brunner_host.py`
- `BRunner/core/nativeBridge.js`
- `BRunner/core/constants.js`

Behavior:
- Schema v2 stores workflow metadata, positioned nodes, edges, and entry node ID.
- V1 steps convert deterministically to v2 nodes and success edges.
- Valid v2 graphs adapt back to a sequential execution view at runtime.
- Initial execution permits exactly one complete acyclic success path.
- Branches, merges, cycles, duplicate IDs, missing nodes, and disconnected nodes
  are rejected.
- Native `UPGRADE_WORKFLOW` atomically replaces a v1 file only after retaining
  `<workflow>.json.v1.bak`.
- Existing backups are never overwritten.
- Invalid/failed upgrades leave the original workflow untouched.

GRAPH STUDIO

Source/build:
- Source: `BRunner/studio-graph-src/`
- Production output: `BRunner/studio-graph/`
- Build config: `vite.studio.config.js`
- Dependencies are pinned in `package.json` / `package-lock.json`:
  React 19.2.7, React DOM 19.2.7, React Flow 12.11.0,
  Vite 8.0.16, and Vite React plugin 6.0.2.
- Build command: `npm run studio:build`
- The accepted sequential Studio remains available and links to Graph Studio.

Implemented graph editing:
- Registry-loaded searchable Node Library.
- Click or drag node creation.
- Node movement, selection, keyboard deletion, handles, and connections.
- Registry-generated Properties panel with contextual fields.
- Zoom controls, minimap, canvas fit, and accessible focus states.
- Node Library and Properties use bounded internal scrolling.
- Direct node remove and midpoint connection remove controls.

Node readability:
- Cards show target, output variable, and relevant configured properties.
- Sensitive HTTP/file payload fields show `Configured`, never raw content.
- Cards show enabled/bypassed/conditional mode.
- Nodes can collapse while retaining name, state, handles, and actions.
- Collapse state persists in v2 workflows.

Canvas organization:
- Legacy v1 workflows receive deterministic non-overlapping layout.
- Workflow layout can be Vertical or Horizontal.
- Arrange reapplies direction, moves handles top/bottom or left/right, and fits
  the canvas.
- Layout direction persists in workflow settings.

GRAPH PERSISTENCE AND LEGACY SAFETY

Files:
- `BRunner/studio-graph-src/src/graphStudioModel.js`
- `BRunner/studio-graph-src/src/GraphStudio.jsx`
- `BRunner/studio-graph-src/src/GraphNode.jsx`
- `BRunner/studio-graph-src/src/RemovableEdge.jsx`
- `BRunner/studio-graph-src/src/nodePresentation.js`

Behavior:
- New/load/save/rename works through the native host.
- V2 save/reload preserves positions, edges, configuration, structured targets,
  variables, settings, bypass mode, layout direction, and collapse state.
- Disconnected/invalid graphs cannot save or run.
- V1 workflows open as clearly marked read-only previews.
- Palette edits, dragging, connections, deletion, bypass, and properties are
  locked for v1 previews.
- Explicit Upgrade to v2 shows the backup filename and unlocks only after the
  native upgrade succeeds.
- The native host must be restarted after host-source changes before testing the
  upgrade command.

BYPASS EXECUTION

Files:
- `BRunner/core/stepBypass.js`
- `BRunner/core/variableRegistry.js`
- `BRunner/background.js`

Modes:
- Enabled: execute normally.
- Bypassed: always skip while preserving graph connections.
- Conditional bypass: evaluate `skipWhen` immediately before the node.

Accepted conditional values:
- true/false
- yes/no
- on/off
- 1/0

Static bypass is checked before other node expressions, so unused missing
variables in a disabled node cannot fail the workflow. Missing or ambiguous
conditional values fail with distinct diagnostics. The legacy `disabled: true`
flag remains supported.

LIVE GRAPH EXECUTION

Files:
- `BRunner/studio-graph-src/src/runtimeProjection.js`
- `BRunner/core/runtimeState.js`
- `BRunner/background.js`

Behavior:
- Graph Studio Run executes the validated current canvas without requiring save.
- Run changes to Stop and uses the shared cancellation path.
- Editing, saving, loading, arranging, deletion, and property changes lock while
  execution is active.
- Authoritative runtime state tracks current, completed, and skipped node IDs.
- Cards show distinct Running, Completed, Bypassed, Failed, and Cancelled states.
- Collapsed cards retain runtime state indicators.
- Completion reports executed and bypassed counts.
- Runtime-only colors/locks are excluded from persisted graph data.

LIVE ACCEPTANCE COMPLETED
- Variables/Data Inspector and typed structured values.
- Variable autocomplete and inline validation.
- Extraction contextual controls and last-run samples.
- Graph palette, drag/drop, connections, properties, delete, and navigation.
- Node summaries, direct remove, bypass, and conditional bypass controls.
- V2 graph save/reload/rename fidelity.
- V1 read-only preview and backup-backed Upgrade to v2.
- Scrollable Node Library.
- Clean legacy auto-layout.
- Removable connections.
- Vertical/horizontal layout and Arrange.
- Persistent node collapse.
- Graph Run/Stop and live node execution states.
- Bounded secret-safe structured execution logs/history, node filtering, node
  selection links, and run summaries.
- Explicit Hand/Selector modes, Space-to-pan, navigation edit guards,
  marquee/additive selection, safe selected-group movement, and keyboard-safe
  deletion.
- Runtime-state colors in the minimap/overview.

CANVAS NAVIGATION AND OVERVIEW UX

Implemented and live accepted:
- Hand/Pan mode must navigate without moving nodes, changing selection, creating
  connections, or triggering destructive controls.
- Selector mode must support single, additive, and marquee selection.
- Dragging a selected node must move the complete selected group while preserving
  relative positions and graph connections.
- Space may temporarily activate Hand mode and must restore the previous mode and
  selection when released.
- Delete/Backspace and other edits must be guarded while navigating so an
  accidental pan gesture cannot mutate the graph.
- The active tool must be visible, keyboard accessible, and announced.
- Minimap/overview node colors must update during execution to match Running,
  Completed, Bypassed, Failed, and Cancelled canvas states, then reset without
  persisting runtime-only color data.

FINAL USER-DIRECTED UI/UX PASS

This is no longer the immediate next phase. Keep the notes below as historical
context for accepted Studio polish work and possible later refinement, but start
the next implementation thread with the Windows companion app transition.

After the functional Milestone 3 acceptance gate, the previous plan was to
perform a dedicated Graph Studio UI/UX refinement pass. Do not guess any future
redesign; if this work resumes later, ask the user for priorities, references,
and detailed direction. Preserve accepted behavior and rerun responsive and
accessibility checks after applying their feedback.

The detailed implementation plan is now in:
- `docs/specs/05_STUDIO_UI_UX_REFINEMENT.md`

The open decisions are confirmed. Slice 1 is complete: workflow-description
persistence, shared global UI preferences/density tokens, icon2 identity, and
the authenticated host-backed `.log` save command are implemented and verified.
The native host must be restarted before live-testing SAVE_EXECUTION_LOG.

Former Slice 2, now deferred: replace Graph Studio's two top bars with the
grouped unified command bar, add centered host status, and move workflow
controls plus saveability state into the pinned-by-default Inspector workflow
view.

DEFERRED VISIBILITY/PRECONDITION REQUIREMENT

The docs now require registry-level capability metadata for nodes that depend on
actual on-screen browser state. Future work must identify and enforce whether a
node requires:
- foreground/active tab;
- focused browser window;
- target visible and unobscured in the viewport;
- target scrolled into view;
- hardware/pointer interaction.

Hover Mouse is the primary example. The runtime must activate/focus/present the
target before dispatch so the node does not fail because the tab or element was
off-screen. Screenshot, hardware keyboard/mouse, drag, and similar nodes require
the same audit. Background-safe HTTP/data operations must not force foreground
activation.

See:
- `docs/specs/01_MULTI_TAB_RUNTIME.md`
- `docs/specs/04_NODE_CATALOG.md`
- `docs/specs/03_STUDIO_GRAPH_UX.md`
- `docs/BRUNNER_MASTER_ROADMAP.md`

NEXT IMPLEMENTATION PLAN

Completed companion foundation slices:
1. Added `BRunner_Host/requirements.txt` with PySide6 and packaging
   dependencies.
2. Added executable-aware `app_paths.py` and active workflow-directory
   resolution.
3. Added shared atomic JSON/text I/O and routed config, workflow save,
   duplicate, rename, upgrade, and execution-log writes through it.
4. Extracted `workflow_repository.py`; WebSocket workflow handlers now delegate
   to the repository.
5. Added schema-v2 settings normalization with legacy aliases and one-time v1
   backup migration.
6. Added approved-directory aliases for read/data-source access while preserving
   legacy `local_file_access` behavior.
7. Added PySide6 `app.py`, `companion_service.py`, and initial desktop shell
   with Status, Workflow Storage, Pairing, Diagnostics, and tray behavior.
8. Added workflow storage location migration helpers and UI controls for
   use-new, copy, move, and default restore.
9. Added dedicated Approved Folders UI with add/edit/remove aliases and
   read/write/recursive controls.
10. Added approved-directory find/write/export service behavior and extension
    graph-authoring access to approved folder aliases for file data sources.
11. Fixed PySide6 enum/import compatibility, host start/stop diagnostics
    refresh, Clear Logs, and stop-process status updates.
12. Added initial `HOST_HELLO` / `host.hello` version and capability reporting,
    with extension-side status hydration and legacy command compatibility.
13. Added initial structured fallback services for `host.window` and
    `host.action`, including foreground-title checks, confidence checks,
    screen-bound coordinate validation, visible pointer/keyboard dispatch
    helpers, v2 request helpers, and deterministic tests.
14. Added the Host Fallback companion tab with enable/threshold/screenshot
    settings, foreground-window status, supported action status, and filtered
    capability activity in Diagnostics.
15. Added initial browser-runtime visible host fallback integration for opt-in
    click, double-click, and type nodes, including content-side target
    preparation, `host.window` / `host.action` calls, and post-action
    verification.
16. Added `Visible Host Fallback Acceptance`, a trusted-click fixture target,
    optional `verificationSelector` / `verificationText` checks, and
    viewport-to-screen coordinate conversion for host fallback preparation.
17. Added initial key-based pairing source pass: extension pairing key is stored
    in `chrome.storage.local`, sidebar exposes host auth status plus
    save/generate/copy controls, host commands wait for `AUTH`, the companion
    Pairing tab has key/port/save/generate/copy/unpair controls, and successful
    auth stores the extension runtime ID internally as the trusted fingerprint.
18. Added initial mapper foundation source pass: pure Mapper Core constants,
    workflow-scoped mapper setting normalization, page-profile normalization,
    placeholder `ComponentRef`, mapper-state serialization, Chrome-storage
    `MapStore`, graph schema v3 `success`/`unresolved` validation, Graph Studio
    source defaults/handles, and a guard preventing unresolved v3 graphs from
    running through the linear executor.
19. Added initial static mapper-core slice: deterministic static page-map
    construction, site/page keys, canonical readable Component IDs with duplicate
    context, compact serializable fingerprints, component caps,
    mutation-heavy `dynamic_deferred` safe decline, primary-first resolution,
    duplicate-primary `ambiguous` state, fallback scoring with
    `mapper.scoring.v1`, and action compatibility filtering.
20. Added initial live content-adapter source pass: content mapper now scans
    document plus reachable open Shadow DOM roots for static candidates, uses
    composed event paths for recording, converts existing resolver evidence into
    compact `mapperFact`, emits `componentRef` for newly recorded DOM steps, and
    Graph Studio auto-adds a `Needs attention` unresolved endpoint for recorded
    mapper nodes so v3 saves remain structurally valid.
21. Added initial mapper coordinator persistence: background routes recorded
    mapper facts through `createMapperCoordinator`, Chrome `MapStore` persists
    workflow-scoped page maps, locked Component IDs survive recorder drift, and
    recorded steps are enriched with Core-produced `ComponentRef` before Studio
    receives them.
22. Added `BRunner_Host/mapper_test.html`, a dedicated mapper acceptance page
    with duplicate Save buttons, stable data attributes, ID/text/order drift,
    open Shadow DOM controls, and a mutation-heavy feed. `start_acceptance_server.ps1`
    now prints the mapper page URL.
23. Added initial mapper-backed execution resolution: background attaches
    stored `MapStore` page-map context before content execution, content
    resolves action targets through the stored Component ID context before
    legacy locators, and browser/visible-host fallback paths return handled
    mapper states (`resolved`, `resolved_with_fallback`, `ambiguous`,
    `not_found`, `dynamic_deferred`) before any page action fires.
24. Added initial mapper graph traversal: schema-v3 workflows now use a narrow
    background traversal path that follows `success` or `unresolved` handles.
    Mapper unresolved content outcomes are logged as handled `unresolved`
    results, update `unresolvedNodeIds` runtime state, avoid visible-host
    fallback dispatch, and route to the explicit `unresolved` edge. The
    generated **Needs attention** node is a safe no-op terminal node. This does
    not implement general graph control flow.
25. Added mapper-aware wait/extraction routing: wait-element conditions now call
    the same mapper-aware `resolveStepTarget` path as actions and host fallback,
    returning handled mapper diagnostics before generic timeout behavior.
    Extraction actions already used the shared action execution path.
26. Added source coverage assertions for mapper graph unresolved traversal,
    visible-host fallback refusal on mapper unresolved states, Graph Studio
    `unresolvedNodeIds` projection, and mapper-aware wait diagnostics. The test
    files were syntax-checked only; the suite was not run because the user asked
    not to run more tests for now.
27. Added `docs/MAPPER_MANUAL_ACCEPTANCE.md` for pre-Inspector mapper manual
    testing, including unresolved routing into **Needs attention**, static
    duplicate-label mapping, dynamic drift, mutation-heavy safe decline,
    infinite-scroll boundaries, and open Shadow DOM checks.
28. Added `BRunner_Host/mapper_stress_test.html`, a fuller mapper stress page
    with static, dynamic, mutation-heavy, infinite-scroll, open Shadow DOM, and
    visible counter/log sections.
29. Added initial pre-Inspector Milestone 2 source behavior: page-map history is
    bounded per workflow/page, static reconciliation now records `same`,
    `changed`, `new`, `removed`, and `ambiguous`, strong matches keep locked
    Component IDs, changed/ambiguous records are marked review-required,
    fingerprint drift produces refreshed map versions, and mutation-heavy pages
    continue to decline honestly as `dynamic_deferred`.
30. Added initial Mapper Inspector source pass: `BRunner/mapper-inspector/`
    opens as an extension page, lists saved workflow map versions grouped by
    website/page, provides Tree/Graph views, shows a Review Queue,
    exposes component locators/fingerprints/capabilities/history, can map the
    active page without the recorder, and can highlight selected components on
    the live website through inspection-only mapper resolution.
31. Fixed Inspector highlight behavior so resolved live elements scroll into
    view before the overlay is drawn. Inspector
    labels now use compact element-type/name labels such as `button_save`
    while keeping canonical Component IDs as details.
32. Added functional Inspector review/policy tools: display alias save without
    changing Component IDs, current-mapping review acceptance, persisted live
    resolver attempts in the detail panel, basic workflow mapper policy editing,
    site sensitive badge, and redacted detail display for sensitive maps.
33. Added richer live candidate attempts and Inspector candidate linking:
    content resolver attempts now include candidate UID, locators, fingerprint,
    capabilities, score, and evidence. The Inspector can link a review-required
    component to a selected live candidate while preserving the canonical
    Component ID and recording the decision/history in the current map version.
34. Review acceptance and live-candidate linking now create fresh Inspector
    review map versions (`map_review_*`) with `previousMapVersionId` and
    `reviewSource`, preserving the prior map version for inspection.
35. Added structured live resolver logs (`mapper.resolver.log.v1`) with
    thresholds, state/reason, selected candidate, runner-up, margin, ranked
    candidates, attempts, confidence, strategy, and value. Inspector displays
    the log beside live resolution details.
36. Removed Website view from Mapper Inspector scope. Tree view now uses a
    reference-aligned dark explorer layout with type icons, indentation, lock
    affordances, compact labels, stable Component IDs as details, and grouping
    modes for captured page structure, regions, and component type. Structure
    mode uses saved DOM-path facts to follow the captured page hierarchy as
    closely as the map data allows. Graph view uses a functional top-down
    hierarchy canvas with Site -> Page -> Region -> Component nodes, connector
    ports, right-angle relationship edges, pan/zoom controls, selected-node
    state, and live highlight selection wiring.
37. Polished the Mapper Inspector right rail so Policy, Review Queue, and
    Component sections have isolated scroll areas, stable section headers,
    wider detail width, carded detail blocks, and bounded JSON panes.
38. Added optional highlight-on-hover preview in the Mapper Inspector. Component
    rows can now preview the same safe live overlay on hover/focus without
    dispatching page actions.
39. Expanded bounded mapper scanning to include user-visible image/media
    elements and visible leaf-text elements with stable signals, so maps can
    track images and copyable text for later click, extraction, and
    screenshot/crop workflows.
40. Added the first host-backed mapper persistence slice: `MapperRepository`
    stores workflow-scoped mapper states as atomic JSON under the workflow
    storage root, the host exposes native mapper-state list/get/save/delete
    commands, and extension core now has a `NativeMapStore` adapter behind the
    same `MapStore` contract.
41. Finished the host-backed mapper persistence hardening source pass: native
    mapper requests use bounded timeouts, pending native bridge calls reject on
    host disconnect/error, `NativeMapStore` exposes typed unavailable/timeout
    status through `MapStoreUnavailableError` and `getStatus()`, mapper states
    retain storage metadata, host saves stamp native revisions, oversized
    normal payloads are capped at 1 MB, and stale saves retain bounded
    last-write-wins conflict metadata. Chrome storage remains the default until
    filesystem-backed persistence is explicitly accepted.
42. Added Mapper Inspector component filtering for the stress-page/manual
    acceptance slice. Tree, Graph, and Review Queue now share component
    search/status filters covering Component ID, alias, display name, role/type,
    capability, status, and review state. Filtered selections clear safely when
    the selected component is no longer visible, and the filter summary reports
    filtered versus total components.
43. Added an explicit Mapper Inspector **Check live resolution** action in the
    Component detail panel, so Review Queue candidate-link flows can fetch live
    resolver attempts and logs even when automatic selection highlighting is
    disabled. Hover-preview requests now cancel quietly on mouse/focus exit so
    stale hover failures do not overwrite the status line.
44. Compact Mapper Inspector UI polish: the Websites rail now consolidates
    repeated maps to one card per base site, retained versions move into a
    selected-site version picker, map retention is capped at three versions per
    page profile even when older settings requested more, Websites/Policy/
    Review Queue/Component panels are collapsible, repeated-review spacing is
    tighter, and top-bar checkbox controls align with their labels.
45. Added full Mapper Inspector Details rail collapse and right-side section
    resizing. Policy and Review Queue can be resized with drag handles while
    the Component section takes remaining space. Added a mapper coordinator
    regression test for same-site page isolation: mapping `/login` and `/home`,
    then changing `/login`, keeps the home-page map untouched while the login
    page receives its own changed version.
46. Split Mapper Inspector map navigation into a clean selected-site Page
    dropdown plus a Version dropdown scoped to that page. Selecting another
    saved page under the same site jumps to that page's latest retained version,
    while the Version dropdown only shows retained versions for the current
    page.
47. Changed Mapper Inspector **Refresh Map** from a saved-state reload into a
    selected-page remap. It passes the selected page map to the background
    mapper so the correct open website tab is rescanned, persists a fresh
    retained version for component-only DOM changes, and keeps inspector-side
    retention capped at three versions.
48. Stress-page UX audit found rough edges to continue fixing: hover/click
    highlight clashes, Tree/Graph switch artifacts, visible-text search gaps,
    poor dense-map contrast/component readability, and weak layouts below
    full-screen desktop widths.
49. Started the Mapper Inspector UX hardening pass: component search now
    normalizes punctuation/underscores and indexes visible/semantic text,
    stable attributes, locator values, hrefs, classes, and titles; selecting a
    component or switching Tree/Graph cancels stale hover previews; Inspector
    highlight hides the recorder hover overlay and draws at the top overlay
    layer; contrast, selected/review states, wrapped controls, and sub-fullscreen
    breakpoints were improved.
50. Added a monotonic Inspector highlight request id through Inspector ->
    background -> content mapper so stale hover overlays cannot overwrite newer
    click/live-resolution highlights on the website. The Inspector shell now
    uses flex height instead of brittle header `calc()` sizing, and Tree/Graph
    view switches clear stale graph panning state.
51. Normalized mapper component ordering to page reading order. Live content
    capture, saved map records, Inspector filters, Structure/Regions/Types tree
    groups, Review Queue, and Graph region/component nodes now sort by document
    bounds top-to-bottom then left-to-right with DOM path fallback. Coordinator
    incoming-fact linking no longer assumes the recorded component is the last
    record, so review/history links still attach correctly after visual sorting.
52. Wired content-side material mutation accounting into Inspector Refresh Map.
    The content mapper tracks bounded material DOM changes after initialization,
    `GET_CONTROLS_TREE` returns the live page mutation count, background map
    creation passes that count into `buildStaticPageMap`, and the Inspector
    subtitle exposes the count/reason so mutation-heavy stress pages can become
    `dynamic_deferred` instead of stale static maps.
53. Added a read-only Mapper Inspector live-status path. `INSPECT_CURRENT_PAGE_MAP`
    scans the selected saved page's open site tab, builds a temporary map
    without saving, compares component count/fingerprint/classification/mutation
    state against the selected retained map, and shows a compact badge plus
    **Check live** button. Refresh remains the only action that writes a new map
    version.
54. Fixed Inspector highlight diagnostics for hidden live elements. Highlight
    and live resolution can include hidden candidates, return `mapperState:
    hidden` with `resolved_element_hidden`, avoid drawing a false overlay, and
    mark Tree/Graph/Review/Component detail with hidden badges after resolution.
55. Hardened click-to-highlight/live resolution for mapped elements that are
    outside the normal candidate scan. Mapper resolution now tries stored
    primary and fallback locators directly across open DOM roots first, supports
    saved ID/name/ARIA/test attributes, label text, visible text, role/text,
    CSS selector, form context, DOM path, and mapper hash locators, reports
    duplicate direct matches as ambiguous, and only falls back to fuzzy
    fingerprint matching when no unique saved locator is available.
56. Scoped Inspector live-resolution and hidden state to the exact retained map
    version plus Component ID, preventing same-named components on different
    pages from sharing diagnostics. Page/version/site switches and disabling
    highlight now clear the website overlay, hover exit restores the selected
    component, and content-side freshness checks after scroll/paint prevent a
    late highlight request from redrawing over a newer selection or clear.

57. Fixed Mapper Inspector dynamic-deferred refresh behavior and Tree usability:
    `MapCurrentPage` now keeps the previous usable map when a live refresh
    produces an unsupported zero-component dynamic-deferred result, retained
    Inspector map lists prefer usable versions over already-saved empty
    unsupported versions for the same page, Tree branches are now expandable and
    collapsible, and component category/status styling is clearer.
58. Added Mapper Inspector saved-version deletion: the Page/Version toolbar now
    has a compact delete-version action that removes only the selected map
    version, preserves other pages and versions for the site, clears live
    overlays, and falls back to the nearest remaining saved version.
59. Fixed appended-feed Tree/Graph presentation: removed historical component
    records are hidden from current-page Tree/Graph views unless the user
    filters for Removed or Review, Review Queue still includes removed records,
    and core map ordering places removed history after live components so stale
    rows do not interleave with appended feed/list items.
60. Fixed sticky dynamic-deferred refresh behavior: read-only live checks still
    use lifetime material mutation counts and can report `dynamic_deferred`, but
    explicit Map/Refresh requests now ask the content script for a settled
    current-DOM snapshot so Tree/Graph can rebuild after a mutation-heavy page
    settles instead of staying pinned to the previous usable map.
61. Fixed Review Queue acceptance semantics: Accept current mapping now writes a
    review decision with previous status, normalizes accepted changed/ambiguous
    records to `same`, preserves accepted removed records as historical removals,
    and prevents explicitly accepted records from remaining in the Review Queue.
62. Added a compact Mapper Inspector app-UI pass: common controls now use
    icon/tooltip affordances, Tree/Graph expose a map color legend, saved site
    rows have direct delete-site actions, the selected toolbar can delete the
    selected page next to the Page selector and one version next to the Version
    selector, checkbox alignment and contrast were tightened, and responsive
    rules now wrap the header, Page/Version controls, filters, Tree, Graph, and
    side rails below full desktop width.
63. Started autonomous mapper reconciliation: strong unique component drift is
    accepted automatically with decision score/margin metadata; weak or close
    historical matches are represented as new live components plus bounded
    removed tombstones, so routine drift no longer creates manual review work.
    Runtime duplicate/close-candidate resolution remains strict and dispatches
    no action.
64. Added deterministic semantic field-matching foundation for future
    `form.fill_from_data`: common field aliases, labels, accessible names,
    placeholders, titles, metadata descriptions, type compatibility, password
    guards, one-to-one assignment, winner margins, and ambiguity diagnostics.
    Added the complete node contract to
    `workflow_nodes_implementation_blueprint.md` section E10 and
    `docs/specs/09_SEMANTIC_FORM_FILL.md`. Runtime, registry, and Studio work is
    explicitly deferred to Milestone 3.4 Phase 3.
65. Added repeated-capture confirmation and count-only reliability metrics for
    autonomous mapper reconciliation. Strong unique historical rebinds keep the
    locked Component ID, start with pending identity confirmation, and confirm
    after the next settled capture with the same live UID. Page maps now expose
    `mapper.reliability.v1` counts/rates for automatic strong matches,
    uncertain-as-new outcomes, Component ID survival, review-required count, and
    pending/confirmed rebinds without copying raw visible text or raw locator
    values into the metrics payload.
66. Fed runtime mapper resolver outcomes back into saved page maps. Content
    execution now returns redacted `mapper.runtime_resolution.v1` outcomes for
    mapper-resolved successes and failures; background persists them through
    `mapperCoordinator.recordResolverOutcome`. Saved maps now keep bounded
    redacted resolver attempts plus runtime counters for fallback recovery,
    ambiguous, not-found, incorrect-action, attempt count, and last attempt.
67. Surfaced saved mapper reliability data in the Mapper Inspector. The Policy
    panel now shows compact page-level reliability chips, and the Component
    panel shows reconciliation decision/confirmation plus the last saved
    redacted runtime attempts for that component. The UI uses only counts,
    scores, evidence labels, timestamps, and locator strategies, not raw
    selector/text payloads.
68. Reduced Inspector hover/click highlight fighting. Hover previews now skip
    the already-selected row, cancel pending selected-highlight restore when a
    new hover starts, and debounce selected-highlight restoration after hover
    leaves. Source tests guard the timer and restore behavior; live visual
    acceptance is still needed in the extension window.
69. Reduced Tree/Graph switching artifacts. The Inspector now clears transient
    graph panning state before view switches and selected-map rerenders, so
    drag/pan classes and state should not survive into a different view or a
    rebuilt graph. Source tests guard the reset helper; live visual acceptance
    is still needed.
70. Strengthened Mapper Inspector component search. Search now supports exact
    phrase or all-term matching across visible semantic text, structural labels,
    locator data, reconciliation/confirmation metadata, Component IDs, hidden
    state, status, and capabilities. This should make partial visible text
    searches such as feed item text work across Tree, Graph, and Review Queue.
71. Tightened Mapper Inspector responsive control wrapping. The map title,
    subtitle, live-status badge, page/version selectors, and component filter
    summary now shrink or wrap cleanly at mid-size and narrow widths, with a
    source test guarding against toolbar clipping regressions.
72. Added the platform-specific mapper profile track to docs. Chat/social apps
    such as WhatsApp Web, Facebook, Instagram, and Reddit must not be treated as
    generic static pages when grouping/tracking evidence is poor; virtualized
    feeds, repeated cards, conversation/thread regions, composers, action bars,
    unread badges, and ephemeral dynamic content need profile-specific rules or
    conservative unresolved/dynamic-deferred outcomes.
73. Started the Mapper Inspector small-screen app-layout track. Added a mobile
    viewport, documented the small-screen plan in
    `docs/specs/10_MAPPER_INSPECTOR_SMALL_SCREEN.md`, and changed collapsed
    Websites/Details panels to horizontal disclosure bars below the stacked
    breakpoint instead of clipped vertical rail labels.
74. Continued small-screen Inspector implementation. Mobile CSS now gives major
    panels `min-width: 0`, wraps saved-site/component labels, keeps detail
    cards and badges from overflowing, and rearranges Graph controls/legend into
    stacked mobile rows. Source tests guard these responsive app-layout rules.
75. Added `docs/specs/11_MAPPER_PLATFORM_APP_PROFILES.md` for WhatsApp/chat and
    social-media mapper profiles. It defines chat/thread/composer/message
    regions, social feed/card/action-bar regions, virtualized loaded-window
    limits, ephemeral dynamic content handling, Inspector disclosure needs, and
    fixture/live acceptance gates before support is claimed.
76. Added the local platform-profile mapper fixture
    `BRunner_Host/mapper_platform_profiles_test.html`. It simulates a
    WhatsApp-like chat shell with conversation list, active thread, loaded
    message window, composer, thread swaps, older-message loading, and
    ephemeral unread/timestamp changes; it also simulates a social feed with
    tabs, loaded feed cards, scoped action bars, a comment composer, feed-window
    appends, and dynamic counters/timestamps. Source tests and docs now include
    the fixture.
77. Started platform-profile source metadata. Content mapper snapshots now emit
    redacted `mapper.platform_profile.v1` hints for chat/social pages using
    structural signals and loaded-window counts only. Background forwards the
    hint into `buildStaticPageMap`, Mapper Core sanitizes and stores it on the
    page map, and the Inspector shows the detected family/confidence in the map
    subtitle plus a compact Policy-panel summary. This is diagnostic metadata
    only; profile-specific grouping/resolution remains a future slice.
78. Added component-level platform scope facts. Content component facts now emit
    redacted `mapper.platform_scope.v1` structural evidence for chat regions
    such as conversation list, active thread, message row, and composer, and
    social regions such as profile tabs, feed card, loaded feed window, and
    comment composer. Mapper Core sanitizes the scope and uses family/region
    plus thread/container tokens as structural identity context, improving
    readable Component IDs and Tree Structure grouping for repeated chat/social
    controls without storing raw chat/post text. Full profile-specific resolver
    behavior is still not implemented.
79. Surfaced platform scope in Mapper Inspector grouping. Structure mode now
    prefers `mapper.platform_scope.v1` paths for chat/social components before
    falling back to DOM paths, and Regions mode labels groups with
    family/region/thread/container scope. This makes the platform fixture easier
    to inspect while preserving generic behavior for ordinary pages.
80. Added selected-component platform scope diagnostics. The Mapper Inspector
    Component panel now shows a compact Platform Scope block for non-generic
    scoped components, including sanitized family, region, durability,
    thread/container token, repeated kind, and loaded-window index. Generic
    components keep the existing compact detail layout, and no page content is
    exposed by this diagnostic block.
81. Added conservative platform-scope identity boundaries. Static
    reconciliation and both pure/live mapper resolution now reject scoped
    candidates from another platform family, region, thread, container, or
    repeated-kind boundary before locator/fingerprint scoring. Contradictions
    become new/not-found rather than cross-thread/card rebinds, including when
    a Component UID is incorrectly reused. Loaded-window index remains
    diagnostic so virtualized-window shifts do not break the same scoped target.
82. Completed open-shadow path persistence and observation. Component facts now
    carry bounded composed shadow boundary paths, runtime DOM paths include open
    shadow transitions, and the mapper observes discovered open roots so nested
    mutations trigger rescans.
83. Added bounded region dynamics and `hybrid_dynamic` maps. Mutation-heavy
    pages with identifiable dynamic/loaded-window regions retain current
    components instead of collapsing to an empty unsupported map; unbounded or
    over-cap pages still use `dynamic_deferred`.
84. Excluded ephemeral chat/social descendants from durable identity text.
    Unread badges and timestamps remain explicit dynamic context records while
    conversation rows gain thread scope and stable controls avoid identity churn.
85. Added a phone Graph hierarchy fallback and Region Dynamics diagnostics.
    Phone widths use a compact selectable region/component list; larger widths
    retain the graph canvas. Dynamic context is searchable and visibly labeled.
86. Added known-host chat/social inference and conservative inferred scopes for
    WhatsApp, Messenger, Telegram, Slack, Discord, Facebook, Instagram, and
    Reddit. Repeated rows/cards lacking a durable hashed container are protected
    unsupported instead of receiving unsafe generic actions.
87. Implemented global/site/page mapper mode overrides. Manual-map-only mode now
    prevents recording from creating or mutating page maps until the page is
    explicitly mapped; Inspector Policy exposes all override levels.
88. Added same-origin frame support. Content scripts run in all frames,
    components store stable frame paths, Inspector mapping aggregates accessible
    frames, and highlight/execution rediscover current frame IDs by path.
    Cross-origin frames remain protected unsupported. The stress harness now
    includes an interactive same-origin frame fixture.
89. Added `docs/MAPPER_TODO_STATUS.md` as the consolidated mapper checklist,
    separating completed source work, live extension acceptance, product
    decisions, and intentional security limits.
90. Added generic repeated-feed scope. Stable card/row keys are stored as
    redacted pinned item tokens and remain valid across loaded-window index
    shifts. Unkeyed repeated patterns are retained for inspection but actions
    return `repeat_condition_required` until an explicit condition identifies
    the intended item. Inspector Structure/Regions and Component details expose
    the Repeat Scope and loaded-only policy.
91. Hardened frame page/routing edges. Same-origin frame recordings inherit the
    top page profile while retaining their frame path, unnamed frame paths use
    realm-safe structural traversal, and visible-host coordinate fallback is
    explicitly blocked for nested frames until frame-to-screen translation is
    supported. Normal browser mapper execution remains frame-routed.

Next isolated implementation slice:
1. Continue Mapper Inspector manual stress-page testing, especially grouped-site
   page/version selection, dynamic DOM Refresh Map behavior, full rail collapse,
   right-section resizing, compact icon/tooltips, color legend, direct
   site/page/version deletion, responsive widths, live highlight behavior, and
   Review Queue candidate-link flows.
2. Decide, after live/manual acceptance, whether to switch mapper persistence
   default from Chrome storage to the filesystem-backed native adapter.
3. Ask the user before removing old source artifacts. Current cleanup
   candidates are `BRunner/New folder/`, `BRunner/todo`, `BRunner/test.html`,
   unused icon alternates `BRunner/icons/icon1.jfif`, `icon3.jfif`, `icon4.jfif`,
   transitional `BRunner_Host/host_ui.py`, and `BRunner_Host/brunner_host copy.py`.

After the companion app is accepted:
- implement the mapper reliability transition from
  `docs/specs/08_MAPPER_RELIABILITY_TRANSITION.md`;
- implement the finalized node set from
  `workflow_nodes_implementation_blueprint.md`;
- keep graph control flow tied to graph traversal, branch handles, cycle
  validation, and iteration limits.

DO NOT IMPLEMENT GRAPH CONTROL FLOW IN THE LINEAR EXECUTOR.
Conditions, loops, merge paths, and error branches depend on graph traversal,
branch handles, cycle validation, and iteration limits.

DEVELOPMENT RULES
- Preserve legacy workflow compatibility and `.v1.bak` safety.
- Add behavior through canonical registry/schema contracts, not action-name hacks.
- Keep secret-bearing payloads out of logs and shared runtime state.
- Keep changes isolated and covered by deterministic plus live acceptance tests.
- Preserve cancellation, persistence, navigation, and tab-recovery behavior.
- When a phase or milestone is completed or materially re-scoped, update the
  roadmap, relevant spec, current handoff notes, and affected user-facing docs
  before moving to the next phase.
- Never perform Git operations; the user owns them.
